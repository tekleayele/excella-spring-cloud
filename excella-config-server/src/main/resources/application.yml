spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tekleayele/spring-cloud-config
          force-pull: true
#        native:
#          searchLocations: classpath:/
      profiles: native
server:
  port: 8888

endpoints:
  restart:
    enabled: true
  refresh:
      enabled: true